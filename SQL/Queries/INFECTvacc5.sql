SELECT
    VACCINE.EMPPAT,
    VACCINE.EID,
    VACCINE.OUTCOME,
    VACCINE.Alias,
    VACCINE.Timestamp,
    VACCINES.VACDESC,
    VACCINES.VACKEY,
    VACCINE.VACDATE

FROM
    INFECTvacc5MR
    INNER JOIN (VACCINE LEFT JOIN VACCINES
        ON VACCINE.VACCINE = VACCINES.VACKEY) ON (INFECTvacc5MR.MaxOfVACDATE = VACCINE.VACDATE)
        AND
        (INFECTvacc5MR.VACKEY = VACCINE.VACCINE)
        AND
        (INFECTvacc5MR.EID = VACCINE.EID)

WHERE
    (((VACCINES.VACKEY)="5"));
