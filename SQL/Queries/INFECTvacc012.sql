SELECT
    VACCINE.EMPPAT,
    VACCINE.EID,
    VACCINE.OUTCOME,
    VACCINE.Alias,
    VACCINE.Timestamp,
    VACCINES.VACDESC,
    VACCINES.VACKEY,
    VACCINE.VACDATE

FROM
    INFECTvacc012MR
    INNER JOIN (VACCINE LEFT JOIN VACCINES
        ON VACCINE.VACCINE = VACCINES.VACKEY) ON (INFECTvacc012MR.MaxOfVACDATE = VACCINE.VACDATE)
        AND
        (INFECTvacc012MR.VACKEY = VACCINE.VACCINE)
        AND
        (INFECTvacc012MR.EID = VACCINE.EID)

WHERE
    (((VACCINES.VACKEY)="12"));
