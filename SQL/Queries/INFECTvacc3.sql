SELECT
    VACCINE.EMPPAT,
    VACCINE.EID,
    VACCINE.OUTCOME,
    VACCINE.Alias,
    VACCINE.Timestamp,
    VACCINES.VACDESC,
    VACCINES.VACKEY,
    VACCINE.VACDATE

FROM
    INFECTvacc3MR
    INNER JOIN (VACCINE LEFT JOIN VACCINES
        ON VACCINE.VACCINE = VACCINES.VACKEY) ON (INFECTvacc3MR.MaxOfVACDATE = VACCINE.VACDATE)
        AND
        (INFECTvacc3MR.VACKEY = VACCINE.VACCINE)
        AND
        (INFECTvacc3MR.EID = VACCINE.EID)

WHERE
    (((VACCINES.VACKEY)="3"));
