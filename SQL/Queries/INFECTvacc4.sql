SELECT
    VACCINE.EMPPAT,
    VACCINE.EID,
    VACCINE.OUTCOME,
    VACCINE.Alias,
    VACCINE.Timestamp,
    VACCINES.VACDESC,
    VACCINES.VACKEY,
    VACCINE.VACDATE

FROM
    INFECTvacc4MR
    INNER JOIN (VACCINE LEFT JOIN VACCINES
        ON VACCINE.VACCINE = VACCINES.VACKEY) ON (INFECTvacc4MR.MaxOfVACDATE = VACCINE.VACDATE)
        AND
        (INFECTvacc4MR.VACKEY = VACCINE.VACCINE)
        AND
        (INFECTvacc4MR.EID = VACCINE.EID)

WHERE
    (((VACCINES.VACKEY)="4"));
