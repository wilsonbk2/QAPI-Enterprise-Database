SELECT
    VACCINE.EMPPAT,
    VACCINE.EID,
    VACCINE.OUTCOME,
    VACCINE.Alias,
    VACCINE.Timestamp,
    VACCINES.VACDESC,
    VACCINES.VACKEY,
    VACCINE.VACDATE

FROM
    INFECTvacc7MR
    INNER JOIN (VACCINE LEFT JOIN VACCINES
        ON VACCINE.VACCINE = VACCINES.VACKEY) ON (INFECTvacc7MR.MaxOfVACDATE = VACCINE.VACDATE)
        AND
        (INFECTvacc7MR.VACKEY = VACCINE.VACCINE)
        AND
        (INFECTvacc7MR.EID = VACCINE.EID)

WHERE
    (((VACCINES.VACKEY)="7"));
