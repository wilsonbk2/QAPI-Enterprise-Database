SELECT
    VACCINE.EMPPAT,
    VACCINE.EID,
    VACCINE.OUTCOME,
    VACCINE.Alias,
    VACCINE.Timestamp,
    VACCINES.VACDESC,
    VACCINES.VACKEY,
    VACCINE.VACDATE

FROM
    INFECTvacc2MR
    INNER JOIN (VACCINE LEFT JOIN VACCINES
        ON VACCINE.VACCINE = VACCINES.VACKEY) ON (INFECTvacc2MR.MaxOfVACDATE = VACCINE.VACDATE)
        AND
        (INFECTvacc2MR.VACKEY = VACCINE.VACCINE)
        AND
        (INFECTvacc2MR.EID = VACCINE.EID)

WHERE
    (((VACCINES.VACKEY)="2"));
