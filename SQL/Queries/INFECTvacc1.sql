SELECT
    VACCINE.EMPPAT,
    VACCINE.EID,
    VACCINE.OUTCOME,
    VACCINE.Alias,
    VACCINE.Timestamp,
    VACCINES.VACDESC,
    VACCINES.VACKEY,
    VACCINE.VACDATE

FROM
    INFECTvacc1MR
    INNER JOIN (VACCINE LEFT JOIN VACCINES
        ON VACCINE.VACCINE = VACCINES.VACKEY) ON (INFECTvacc1MR.MaxOfVACDATE = VACCINE.VACDATE)
        AND
        (INFECTvacc1MR.VACKEY = VACCINE.VACCINE)
        AND
        (INFECTvacc1MR.EID = VACCINE.EID)

WHERE
    (((VACCINES.VACKEY)="1"));
